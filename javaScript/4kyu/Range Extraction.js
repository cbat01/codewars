function solution(list){
    let res = '';
    list.map((val,i) => {
        if(val + 1 !== list[i+1]){
            res += val;
        }else{
            mark = val
        }
    })
    // unfinished!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
}